{
  "name": "fruit-10-plus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "dev": "vite",
    "check": "node --check src/main.js && node --check src/game.js && node --check src/config/levels.js && node --check src/core/grid.js && node --check src/platforms/browser.js && node --check scripts/package-yandex.mjs",
    "test": "node --test test/grid.test.js test/levels.test.js",
    "build": "vite build",
    "preview": "vite preview",
    "package:yandex": "node scripts/package-yandex.mjs",
    "release:yandex": "pnpm run check && pnpm run test && pnpm run build && pnpm run package:yandex"
  },
  "devDependencies": {
    "archiver": "^8.0.0",
    "vite": "^8.2.1"
  }
}
